---
period: 2026-02
type: Monthly Report
tags: [Trends, Learning Log]
---

# 🗓️ Intelligence Log: 2026-02

## 🔭 The Observatory (Industry Radar)

> 捕捉行业宏观信号，定义本月技术主旋律。

* **Dominant Theme:** [例如：Reasoning Models & Scaling Law 2.0]
* **The Signal:** [DeepSeek-R1 证明了纯 RL 训练推理模型的可行性，行业焦点从 Pre-train 转向推理侧 Scaling。]

> Use an image to visually summarize these themes and signals. Ensure it has descriptive alt text and supplements the written text.

---

## 🏗️ Technical Review: Models & Directions

> 核心技术点追踪。

### 1. 基座模型追踪 (The Clay)

| 模型名称 | 核心技术创新 (Mechanism) | 关键能力/指标 (Capability) | 业务应用/启发 (Value) |
| :--- | :--- | :--- | :--- |
| [LongCat-Flash-Thinking-2601](https://arxiv.org/abs/2601.16725) | **[Infra] 面向“多环境、多回合、长尾交互”的异步 RL 基建（DORA 扩展）**：<br><br> **多版本+全流式异步流水线**：rollout / env 执行 / reward 计算按 sample 粒度流式推进，支持 trajectory 完成即入队，训练端满足条件即可开训 <br> 宣称整体 **2–4×** 快于同步。<br> **超大规模环境并发**：支持最多 **32,000** 环境并发、约 **400** 物理机规模 <br> 通过 Lightweight-RolloutManager + RolloutController 分片、扩展 PyTorch RPC 做 CPU idleness-aware 调度。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226191942187.png) <br> **PD（Prefill–Decode）解耦 + KV-cache CPU swapping**：应对长上下文/多请求下 expert-parallel 组内负载不均与显存紧约束，用异步 chunk 传输 KV 并用 CPU 驻留 KV 规避重算。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226192014392.png) <br><br> **[Data] “环境可执行/可验证”驱动的 agentic 数据与任务规模化**：<br><br> **Mid-training 结构化 agentic 轨迹补齐稀缺性**：分阶段长上下文激活（32K/128K 阶段 **500B tokens** + 256K 阶段 **40B tokens**），同时注入合成的结构化 agentic 轨迹，为后续大规模 RL 冷启动。<br> **Text-driven synthesis**：从教程/流程文本中做 **workflow 挖掘→工具 schema 提取→多轮对话轨迹合成与精炼** <br> 再做 **Tool decomposition**（把参数隐藏进环境、迫使交互补全）与 **Reasoning decomposition**（每步生成候选、训练决策选择）。<br> **Environment-grounded synthesis**：从工具定义生成轻量 Python 可执行环境 <br> 构建 **工具依赖有向图**，采样 tool-chain，**反向合成 prompt**，并通过执行与环境状态校验保证正确性。<br> **Planning-oriented augmentation**：把线性轨迹改造成“分解+早期动作选择” “多候选决策选择”的规划数据。<br> **Cold-start 质量控制**：通用思考数据用 **K-Center-Greedy + sliding-window PPL（512-token window 的 max avg PPL）** 做覆盖+信息量联合选择。tool-use 轨迹用 rubric 校验 + turn-level 质检，并对失败/格式问题 turn 做 **loss masking**。<br><br> **[Train] 统一多域 agentic RL 策略 + 稳定性与鲁棒性共训**：<br><br> **目标函数：GSPO（Group Sequence Policy Optimization）** 做序列级优化，更适合长轨迹 <br> 偏 outcome supervision，弱化对长轨迹的惩罚。<br> **两轴 curriculum**（难度/能力要求）+ **动态预算分配**（按实时 pass rate 等指标给 task 估值，近似实现用 pass-rate/历史成功率的 oversampling）。<br> **Self-verification 辅助阶段**：在生成停滞时触发，让模型评估自身 rollout 作为辅助信号加速收敛。<br> **多域环境混训稳定化**：用“域/数据类型分离 oversampling 比例”防止长尾域拖慢异步流水线，同时维持训练混合近似均衡。<br> **显式噪声建模的 Robust RL**：分解真实世界噪声（instruction noise、tool noise），用 curriculum 逐步加噪，但保持任务仍可解。<br> **长程交互的 context management**：summary-based（基于 ReSum，阈值 **80K tokens**，冷启动合成 **15K** summarization 数据）+ discard-all 的混合策略。<br><br> **额外机制**：Zigzag Attention 用 MLA + Streaming Sparse Attention，约 50% 层替换为稀疏层 <br> block size 128、1 sink block + 7 local blocks（每层有效 1,024 token）。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226192412874.png) | **特点能力 A：Agentic Search / Browsing 强，且对长程交互做了系统性 context 管理**：<br><br> BrowseComp **56.6 / 73.1**（无 / 有 context management，Pass@1） <br> BrowseComp-ZH **69.0 / 77.7** <br> RWSearch **79.5**（Pass@1）。<br> 评估前提：BrowseComp 系列区分是否启用 context management <br> RWSearch 统一不启用。<br><br> **特点能力 B：Agentic Tool-Use 的多域泛化 + 噪声鲁棒**：<br><br>![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226192126270.png) <br> 评估前提：噪声集是对同任务多次随机注入噪声后取平均 <br> 部分基准在报告中对 simulator/环境问题做了修订以提高可控性。<br><br> **代价/约束**：<br><br> 能力高度依赖“**可执行环境 + 自动验真/rubric + 大规模并发调度**”，复现门槛更像搭 RL 平台而非仅训练模型。<br> Heavy Thinking / 并行思考会显著放大推理成本与延迟（宽度×深度扩展）。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226192243764.png) ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226192309761.png) | **可复用 post-training 路线**：<br><br> **Mid-training 先“教会交互形态”**：在长上下文 staged 激活同时引入中等规模结构化 agentic 轨迹，降低后续 RL 的探索成本。<br> **多域统一 RL**：用 GSPO + 两轴 curriculum +（近似）动态预算分配 + self-verification 辅助阶段，专门面向长轨迹与长尾环境交互。<br> **Robust RL**：把噪声（instruction/tool）显式参数化，按 curriculum 逐层加噪，核心是“不破坏可解性”以避免错误负奖。<br><br> **数据合成与筛选“配方”**：<br><br> **环境缩放（Environment Scaling）**：域规格 → 自动合成工具/DB schema/实现 → 单测 + 调试 agent 校验（报告称 schema→实现成功率 >95%）→ 构建工具依赖图 → 从可执行 tool-chain 出发 BFS 式扩展，只在依赖满足时加节点，确保 executability <br> 再生成“任务描述 + user profile + 评测 rubrics”，并对 rubrics 做一致性校验。<br> **两路 agentic 合成**：Text-driven（挖流程→显式工具调用→轨迹精炼）+ Environment-grounded（执行验证闭环）。<br> * **质量控制细节**：rubric outcome 验真 + turn-level 过滤/format 校验 <br> 对低质量 turn 做 loss masking（保留上下文但不让坏动作进梯度）。<br> **通用思考冷启动选数**：KCG + sliding-window PPL（抓局部难点 token）用于“覆盖+信息量”的下采样。<br><br> **落地依赖与改造成本**：<br><br> 必备：可复现沙箱/执行器（代码、工具调用）、统一工具接口、任务判定器/Verifier、评测 harness、轨迹日志回放与失败归因 <br> 以及能支撑多环境并发的调度系统。<br><br> **可迁移结论**：<br><br> 优先“**可执行可验证任务集 + rubric/验真**”，再 RL。<br> 做鲁棒性不要只靠线上兜底：把真实噪声类型拆解后“**训练时显式注入 + 课程式加难**”，通常比上线后被动适配更可控。 |
| [DeepSeek-OCR 2: Visual Causal Flow](https://arxiv.org/abs/2601.20552) | **[Infra] “视觉 token 先双向、再因果重排”的 LLM-style 编码器**：<br><br> **核心瓶颈**：传统 VLM 将 2D patch 以固定 raster-scan 顺序 + 固定位置编码喂给 LLM（1D 序列建模），对复杂版面引入不合理顺序偏置。<br> **DeepEncoder V2 机制**：把编码器的 CLIP/ViT 部分替换为紧凑 LLM 架构（用 Qwen2-0.5B 作为初始化/骨架），并引入两类 token：<br> **视觉 tokens**：保持 ViT-like **双向注意力**（全局感受野）<br> **causal flow queries**：采用 **因果注意力**，每个 query 可 attend 全部视觉 tokens + 之前的 queries，从而“逐步、语义驱动地重排视觉信息”。 ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226195927683.png) <br> **注意力 mask 关键设计**：块状拼接的双向 + 下三角因果 mask。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226200047250.png) <br> **只输出 queries 给 LLM 解码器**：编码器输出的一半（causal flow tokens）作为压缩后的、带顺序的视觉表示，喂给下游 DeepSeek-MoE decoder。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226200021456.png) <br><br> **[Infra] 高压缩视觉 tokenizer + token budget 约束**：<br> 视觉 tokenizer 沿用 DeepEncoder：**80M 参数 SAM-base + 2 层卷积**，通过 window attention 实现约 **16× token compression**，显著降低后续全局 attention 的算力与激活内存。<br> 视觉 token 控制在 **256–1120**：global view 1024×1024 → 256 <br> local crop 768×768（0–6 个）→ 每个 144，总数 $k \times 144+256\in[256,1120]$。<br><br> **[Data] 数据问题与配比的“小改动大收益”**：<br> 数据源基本沿用 DeepSeek-OCR，**OCR 占训练混合 80%**。<br> 关键改动 1：对 OCR 1.0 按内容分桶更均衡采样（text/formula/table = **3:1:1**）。<br> 关键改动 2：layout 标签精炼（合并语义相近类别，如 figure caption/title）。<br><br> **[Train] 三阶段训练把“重排能力”单独做出来**：<br> Stage-1（encoder pretraining）：语言建模目标（next-token），encoder+轻量 decoder 联训 <br> 分辨率 768/1024 两个 dataloader <br> batch 640，**40k iters**，sequence packing 到 8K，约 **100M image-text pairs**。<br> Stage-2（query enhancement）：冻结视觉 tokenizer，联合优化 LLM-style encoder + LLM decoder <br> multi-crop 合并数据 <br> **4-stage pipeline parallel**（tokenizer / encoder / DeepSeek-LLM 分两段），global batch 1280，**15k iters**。<br> * Stage-3（decoder specialization）：冻结全部 DeepEncoder V2，仅训 LLM <br> **20k iters**。<br><br> **TL;DR**：把“2D 文档阅读顺序”显式建模为 **编码器内的因果重排（queries）**，再交给自回归解码器做序列推理，用两级 1D causal 结构逼近“真实 2D 阅读逻辑”。 | **特点能力 A：文档解析总体质量显著提升（高压缩 token 预算下）**：<br> ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226200146115.png) <br><br> **特点能力 B：阅读顺序（reading order）更强，符合“因果视觉流”目标**：<br> ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226200202247.png) <br><br> **特点能力 C：线上可观测质量更稳（重复率下降）**：<br> ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260226200214521.png) <br><br> **短板/代价**：<br> **token budget 上限较低时对极密文本可能不利**：论文指出 newspaper 文本 ED 仍可 >0.13，原因包括（1）local crops 数不足 <br> （2）相关训练样本仅约 **250k**，覆盖不足。<br> 该范式的收益强依赖“复杂版面/顺序敏感”任务 <br> 对纯自然图像理解未必同等显著。 | **可复用 post-training/训练组织路线**：<br> 把能力拆成 3 段：<br> 先把 encoder 的“压缩 + 重排”学出来（Stage-1）<br> 再做 encoder/decoder 的接口对齐与 query 强化（Stage-2）<br>  最后冻结 encoder、只训 decoder 提吞吐（Stage-3）<br> 当引入“新表征形态”（这里是 reordered queries），**单独安排一个“冻结上游、下游适配”的 continue-train 阶段**，常比端到端硬怼更稳、更省 FLOPs。<br><br> **数据合成与筛选“配方”（文档场景）**：<br> 不是“大换数据”，而是对 OCR 数据做 **内容分桶均衡采样** + **标签体系合并去噪**（layout 类别归并），用更少改动提升泛化。<br> 任何结构化视觉任务都值得先做“按结构类型的 sampling budget”，而不是只追总量。<br><br> **落地依赖与改造成本**：<br> 训练侧：需要较强的分布式能力。<br> 推理/服务侧：需要 multi-crop 视图策略与 visual token 上限控制。<br> 评测侧：配套 OmniDocBench 类的 parsing harness，否则很难定位“顺序建模”是否真起效。<br><br> **可迁移结论**：<br> 当任务天然存在“阅读逻辑/因果顺序”，可以把“顺序”从 prompt/后处理前移到 **encoder 内的可学习重排**，并用“只喂 queries 给 LLM”控制 token 成本。<br> 用 LLM-style encoder + modality-specific queries 的方式，有潜力做 **统一多模态编码器**，从架构上继承 LLM 社区的 MoE/高效 attention 等优化红利。 |
| [Kimi K2.5: Visual Agentic Intelligence](https://arxiv.org/abs/2602.02276v1) | **[Infra] 多模态训练效率与“并行 agent”推理形态** <br> Agent Swarm + PARL：把“串行工具调用”变成“学习到的并行调度”：<br> Orchestrator 可创建/委派 frozen subagents，异构子任务并发执行 ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302134733043.png) <br> 训练只更新 orchestrator（subagent 冻结且轨迹不进优化目标），规避 credit assignment 模糊与训练不稳定 <br> 用 *critical steps* 作为并行 setting 的资源约束：$$\text{CriticalSteps}=\sum_{t=1}^{T}\left(S_{\mathrm{main}}^{(t)}+\max_{i}S_{\mathrm{sub},i}^{(t)}\right)$$ 直接优化端到端延迟而非“刷并发数” ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302134755979.png) <br><br> MoonViT-3D：图像/视频统一的原生分辨率视觉 encoder：<br> NaViT packing 支持可变分辨率输入 <br> 轻量 3D 压缩：连续 4 帧分组、共享 MoonViT 编码，patch 级时序平均 → 在同一上下文窗口可处理最长约 $4\times$ 视频长度（且图像/视频权重共享）<br><br> **[Data] 早期融合 + 全程恒定视文比例的 joint pretrain** <br> 约 15T 混合视文 tokens 的 joint 预训练 <br> 关键 ablation 结论：在固定视文 token 预算下，“早期注入 + 较低 vision ratio + 全程恒定比例混训”优于“后期高比例注入” ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302134507619.png) ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302134908216.png) <br> 预训练三阶段：![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302134814540.png) <br><br> **[Train] “零视觉 SFT 冷启动” + joint text-vision RL + 并行 orchestrator RL** <br> Zero-vision SFT：只用文本 SFT 数据激活视觉推理/视觉工具使用（用 IPython 程序化图像操作做代理），并观察到“加入人造视觉轨迹的 SFT 反而伤泛化” <br> Outcome-based visual RL：针对必须看图才能对的任务做结果导向 RL ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302134659109.png) <br> Cross-modal transfer：视觉 RL 不仅不伤文本，反而带来文本基准提升 ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302134712960.png) <br> Joint multimodal RL：按“能力域”（knowledge/reasoning/coding/agentic）组织，而非按模态切专家 <br> GRM（Generative Reward Model）跨文本与多模态轨迹统一打分 <br> PARL reward 设计抑制两类局部最优：serial collapse 与 spurious parallelism <br> $\lambda_1,\lambda_2$ 随训练退火至 0 <br> RL 稳定性：token-level log-ratio clipping，并继续使用 MuonClip optimizer <br> Token efficiency：Toggle 交替做预算约束与放开推理，平均输出 tokens 降约 25%–30% 且性能基本不掉 | **特点 A：视觉×文本的“互促式”联合优化** <br> 定义：joint pretrain + joint RL 让视觉能力提升同时反哺文本能力，避免传统 VLM 的“加视觉伤语言”权衡。 <br> cross-modal transfer，视觉 RL 后文本提升：<br> MMLU-Pro：84.7 → 86.4（+1.7）<br> GPQA-Diamond：84.3 → 86.4（+2.1）<br> LongBench v2：56.7 → 58.9（+2.2）<br><br> **特点 B：agentic 能力强，且“并行化”带来同时的质量与延迟收益** <br> 定义：通过 Agent Swarm 学到动态拆解 + 并行执行，提升复杂检索/研究任务的 item-level 指标，并显著缩短 wall-clock。<br> 单体与 Swarm 对比：<br> BrowseComp：60.6 → 78.4（+17.8）<br> WideSearch Item-F1：72.7 → 79.0（+6.3）<br> 延迟：WideSearch 场景最高宣称 $3\times–4.5\times$ 加速（相对 single-agent 达到目标 F1 的时间）<br><br> **特点 C：端到端综合强（coding / OCR / 长视频 / 电脑操作）** <br> SWE-Bench Verified：76.8 <br> OCRBench：92.3 <br> InfoVQA(test)：92.6 <br> OmniDocBench 1.5：88.8 <br> LongVideoBench：79.8 <br> LVBench：75.9 <br> OSWorld-Verified（纯 GUI 行为）：63.3 <br> WebArena：58.9 <br><br> **短板/代价**  <br> 训练/系统门槛高：15T 级视文混训 + 多阶段 RL（含 GRM）+ 多 agent 环境，研发成本与复现门槛显著高于“单阶段 SFT” <br> 能力强依赖“可执行/可判定”的环境：PARL、outcome-based RL、工具与 GUI 测试都要求 harness/执行器/日志回放 <br> 视觉极限感知仍不“碾压”：如 ZeroBench 仍为 9（w/ tools 11），提示在“几乎不可能视觉”任务上仍受限 | **可复用 post-training 路线（从“冷启动”到“joint RL”再到“并行 orchestrator”）** <br> 冷启动：**Zero-vision SFT**（文本-only SFT 激活视觉工具/视觉推理）<br> 关键启发：如果 joint pretrain 的跨模态对齐足够强，后训练阶段不必强塞“少量人工视觉轨迹”，反而可能伤泛化 <br> 视觉强化：Outcome-based visual RL（grounding/计数、图表文档/OCR、vision-critical STEM）<br> 统一强化：Joint multimodal RL（按能力域组织 expert <br> GRM 跨模态统一评价） <br> 并行化强化：**PARL 只训 orchestrator，subagent 冻结** → 降低 credit assignment 难度与不稳定性 <br><br> **数据合成与筛选“配方”** <br> 训练闭环：从 RL 轨迹中抽取高质量轨迹做 RFT/过滤（论文描述为 self-improving pipeline 的关键组成）<br> 开放式任务：用 GRM（多 rubric）做细粒度偏好评估，降低 reward hacking 与单一偏好过拟合 <br><br> **落地依赖与改造成本** <br> 训练侧：统一的 agentic RL 环境、可验证 reward、工具接口、GUI/浏览环境、评测 harness <br> 推理侧：并行子任务调度器（线程/进程/分布式）、子 agent 生命周期管理、结果聚合与冲突消解、可观测性（事件流/trace/回放） <br> 数据侧：可复用的合成-验真-过滤链路（尤其是可执行任务的自动判定）<br><br> **可迁移结论** <br> 多模态不是“后期插视觉 token”就能稳：在固定预算下，“早期、低比例、全程恒定混训”更可能得到双模态互促 <br> 多 agent 训练别急着端到端：用“冻结 subagent、只训 orchestrator”的 decouple 思路，往往更稳、更易定位收益来源 <br> 评测/训练的资源度量要从“总步数”升级到“critical path/critical steps”，才能真正驱动低延迟并行策略 |
| [GLM-5](https://arxiv.org/abs/2602.15763) | **[Infra] 让“长上下文 + agentic RL”变便宜且可训：DSA + 异步 RL 基建 + 长序列训练工程** <br><br> 架构与效率：引入 DSA（DeepSeek Sparse Attention）替代长序列的 dense $O(L^2)$ 注意力 <br> 论文称长序列 attention 计算降低约 1.5–2×，并保持 long-context fidelity。<br> MoE 规模/通信取舍：256 experts、80 layers，总参 744B、激活 40B。通过减层来压 expert-parallel 通信开销。<br> 长上下文训练形态：Mid-training 将 context 逐级拉到 200K：32K（1T tokens）→128K（500B）→200K（50B），并在后期上采样 long-context agentic 数据以稳定复杂 workflow。<br> 推理解码提速：Multi-token prediction（MTP）用于 speculative decoding。训练时做“3 层 MTP 参数共享”以减小训推差异，提高 accept length。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302182643933.png) <br> 训练稳定性/注意力实现：采用 MLA（multi-latent attention）并提出 Muon Split（将 $W^{UQ},W^{UK},W^{UV}$ 按 head 拆分做正交化）以弥补 MLA vs GQA 性能差并通过调 head dim / head 数减少 decoding 计算。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302182618836.png) <br> 大模型训练工程：Pipeline ZeRO2 梯度分片 + 双缓冲 <br> Muon 分布式优化器做 zero-redundant all-gather <br> pipeline activation offload（host）+ 输出投影按 sequence chunk 切分降低峰值显存 <br> 长序列训练做 workload-aware reordering、context-parallel 动态分组、分层 all-to-all 重叠通信。<br> Post-training 基建：slime 框架 + “生成/训练解耦”的全异步 RL（rollout 与 trainer 分离，中心 orchestrator 管多任务），用 PD disaggregation（prefill/decode 分离）降低 tail latency <br> rollout 用 FP8 + MTP。<br><br> **[Data] 目标是“真实可执行环境 + 长上下文 agentic 轨迹 + 低噪声高难度推理”** <br><br> 预训练规模：总 token budget 28.5T（base 训练从 27T 语料开始）。<br> Web：在 GLM-4.5 pipeline 上加一个 DCLM embedding-classifier。另用 World Knowledge classifier 从中低质里抽长尾知识。<br> Code：刷新代码快照 + code-web 扩展，fuzzy dedup 后 unique tokens +28% <br> Math&Science：网页/书/论文高质量抽取，LLM 打分只保留“最具教育性”内容 <br> 长文档用 chunk-and-aggregate 打分，明确“严格避免合成/AI/模板数据”。<br> SWE 工程语料：repo 级拼接 code files + commit diffs + issues/PR + 相关文件 <br> 长上下文合成：参考 NextLong/EntropyLong 做 interleaved packing、构造长程依赖 <br><br> **[Train] “SFT → 三段 RL → cross-stage distill”把 agentic 能力闭环化**<br><br> ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302182546789.png) SFT：context 上到 202,752 tokens <br> 引入三种 thinking 形态：Interleaved Thinking（每次回复/工具调用前思考）、Preserved Thinking（跨多轮保留思考块，偏 coding agent）、Turn-level Thinking（按 turn 控制是否推理以控延迟/成本）。 ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302182847089.png) <br> Reasoning RL：基于 GRPO + IcePop 思路显式区分 $\pi^{\text{train}}$ 与 $\pi^{\text{infer}}$，并移除 KL 以加速 <br> 混合 4 域（math/science/code/TIR），用 domain-specific judges/系统给 binary outcome reward。 <br> Agentic RL（异步）：关键机制是 TITO（token-in-token-out）gateway 消除重分词/边界 mismatch。用 Direct Double-sided Importance Sampling + token-level masking 控 off-policy <br> General RL：三维目标（foundational correctness / emotional intelligence / task-specific quality），混合奖励（rule-based + ORM + GRM），并引入高质量人类答案作为“风格锚点”。<br> On-policy Cross-Stage Distillation：以各阶段最终 ckpt 为 teacher，在最后阶段用 teacher gap 做 advantage，缓解多阶段 RL 的遗忘。 | **特点能力 A：真实软件工程与终端 agent 能力强（可执行环境驱动）**<br> **特点能力 B：长上下文 + 工具型 browsing/多步 agent 表现突出** <br> ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302183142496.png) 评估前提：这些结果显式依赖 agent harness（如 OpenHands/Terminus-2/Claude Code）与可执行沙箱/测试并非“纯文本一次性回答”。<br><br> **明确短板/代价**<br><br> “强 agentic”高度依赖：可执行环境、自动判定器、稳定工具接口、以及长上下文推理预算 <br> 长程端到端仍有 gap：![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260302183313451.png) 说明多步错误累积仍是痛点。<br> 系统复杂度显著上升：异步 RL 的 off-policy、tail latency、token 对齐、环境失败噪声处理，都把训练与评测变成“系统工程问题”。 | **可复用 post-training 路线**<br><br> 推荐骨架：SFT（含工具调用/agent 轨迹、长上下文）→ Reasoning RL（可验证任务：math/science/code/TIR）→ Agentic RL（长时交互：SWE/terminal/search）→ General RL（风格与通用对齐）→ Cross-stage Distillation（防遗忘）。<br> 关键不是 RL 名字，而是 **“可验真 reward + 多任务异步 rollout 基建 + 轨迹标准化”**。<br><br> **数据合成与筛选“配方”**<br><br> SWE 环境：从真实 issue–PR 出发，RepoLaunch 自动搭建依赖/生成 test 命令 + 解析日志抽取 F2P/P2P <br> Terminal 环境（Harbor）：seed → LLM brainstorm 草稿 → construction agent 产出 Docker + tests → refine agent 按 rubric 迭代加固（可构建率 >90%） <br> 另一路从高质量 code-web 语料抽取→生成任务→自验证脚本闭环通过才入库。<br> Search 合成：用早期 search agent 轨迹收集/去重 URL →抽实体/关系建 WKG →采样低中频实体做多跳子图→生成问题→三阶段难度过滤 + verification agent 双向验真，拒绝非唯一答案/证据不一致。<br> “错误片段保留但 mask loss”：在 agent 轨迹中保留错误段并在 loss 中 mask，使模型学纠错而不强化错误行动。<br><br>**落地依赖与改造成本**<br><br> 训练侧：异步 rollout/learner 解耦、TITO 网关（token 级轨迹记录） 、off-policy 版本管理与样本丢弃策略、PD disaggregation、FP8 rollout、MTP <br> 评测侧：统一的 harness（Docker/沙箱/测试套件/超时与资源限制）、judge 规范化（prompt+model 固定）、以及可复现的 context management 策略。<br><br>**可迁移结论**<br><br> 优先投资“可执行 + 可判定”的任务集（tests/规则/verifier），把 reward 从主观打分迁到 outcome 监督 <br> 异步多任务 RL 的关键护栏是 **token 对齐（TITO）+ off-policy 控制（版本阈值/重要性采样 clipping/噪声样本剔除）+ tail latency 优化（PD 分离、KV 复用路由）** |
| [Step 3.5 Flash](https://arxiv.org/abs/2602.10604) | **[Infra] 以“低延迟 agent 交互”为目标的 MoE 系统-模型协同设计**：<br> 196B 总参的稀疏 MoE，但每 token 仅激活 11B（$topk{=}8$ experts），重点面向多轮交互场景的 wall-clock latency。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120449250.png) <br> Muon 与 ZeRO-1 的梯度重排/resharding 以避免“重建全量梯度”的通信翻倍；注意力与 MoE 的 kernel 融合（含 fused gather/scatter + grouped GEMM）<br><br>**[Infra/Arch] “3:1 SWA/Full + MTP-3”加速长上下文与解码**：<br>采用 $3{:}1$ Sliding-Window Attention/Full Attention 的混合布局（S3F1，SWA window $W{=}512$），并针对混合注意力带来的质量回落，用两招补偿且几乎不增加实际延迟：SWA query heads 64→96；head-wise gated attention（替代固定 sink token）。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120121479.png) <br> 同时引入 Multi-Token Prediction（MTP-3）以配合 speculative decoding，降低多轮 agent 交互的解码成本。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120032012.png) <br><br>**[Infra/MoE] 面向在线部署的 EP-straggler 抑制**：<br>除“全局”loss-free balancing 外，引入 EP-group 级别的平衡损失，减少 micro-batch 层面的 routing 偏斜导致的 straggler。<br><br>**[Data] 以 agentic 工作流为中心的数据地基与增广**：<br>训练节奏：预训约 17.6T tokens（4k→32k，annealing 向 code/PR 等倾斜）；mid-training 约 750B tokens（32k 强化→128k 长上下文专项，带 replay 以减分布漂移）。<br><br>**[Train] 把“MoE 稳定性”工程化：诊断指标 + 定点干预 + 可扩展 RL**：<br>预训全程 Muon：Muon 的数值敏感性用 Polar Express 并将该迭代环节单独 cast 到 fp16 来消除偶发不可恢复 spike ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120146409.png) <br> 针对深层 MoE 的局部 activation blow-up，实证更有效的是 expert 内 activation clipping（weight clipping 只能延缓）![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120206632.png) <br> 后训方面提出 MIS-PO：用 token/trajectory 两级离散过滤替代连续 importance weighting，显著降低长轨迹 off-policy 梯度方差；并配合 truncation-aware value bootstrapping、routing confidence 监控等稳定化组件。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120230745.png) ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120641318.png) | **特点能力 A：高性价比的推理/代码/agent 综合强度（以 11B active 达到 frontier 级别）** <br> ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120353241.png) ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120416738.png) <br><br> **特点能力 B：长上下文 agent 工作负载的“低延迟优先”设计更贴近真实交互** <br> 架构侧 S3F1 混合注意力 + GQA-8（对 8-GPU 节点 KV cache/TP 对齐）+ MTP-3，目标是 prefilling 和多轮解码整体 wall-clock 更优。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120511013.png) ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120551010.png) ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120102254.png) <br> 评估前提：此类优势通常在“长上下文 prefilling + 多轮短解码”的 agent 会话形态下显著，且强依赖推理引擎与 speculative/verification 的实现质量。<br><br> **明确短板/代价** <br> token efficiency 仍偏弱；能力提升路径高度依赖可执行环境/验证器/高吞吐 RL 基建；MoE + 长上下文 + RL 的稳定训练需要重监控与多处工程化干预。 | **可复用 post-training 路线（从“统一 SFT”出发的专家化→蒸馏→可扩展 RL）** <br> 统一 SFT 打底（两阶段：多域覆盖 + 提升 reasoning density 的 OOD 注入），然后做“按域构建 Expert Models”的 RL，再通过 self-distillation 把专家轨迹收敛回单一 student；最后用可扩展 RL 框架持续自改进。![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120324810.png) ![image.png](https://raw.githubusercontent.com/kyriekevin/img-hosting/main/imgs/20260304120256068.png) <br> 把“专家化（分域迭代快）”与“统一体（上线/维护成本低）”用蒸馏联通，而不是长期维护多模型碎片。<br><br> **数据合成与筛选“配方”** <br>工具学习：用 FSM 把 tool-use 拆成原子 intent，走 sample→execute→verify→rejection sampling 的闭环，硬性用真实环境的确定性反馈验真，专门过滤“幻觉式工具调用/伪检索”。<br>代码 agent：把“环境构建”当作一等能力，通过可执行反馈与轨迹规范化提高监督密度；并引入 cross-task memory（历史成功构建 few-shot）与 loop detection。<br>搜索/研究 agent：图结构/多文档合成保证 multi-hop；用强模型做“无需工具即可解”的反筛；再用 rubric-based judge 严格约束报告结构与指令遵循。<br><br> **奖励/对齐范式的工程要点** <br> RLVR：为每类任务配 verifier（规则/执行器/模型判别）给确定性或半确定性奖励。<br> 非可验证：GenRM（pairwise generative RM）+ Bradley–Terry 转换；显式 length control；对伪造引用、过度自信、语言不一致等置零；再叠 MetaRM 抑制“伪推理”。<br><br> **落地依赖与改造成本** <br> 可执行沙箱（代码/终端/工具调用）、统一工具接口与会话管理、自动验真器/判题器、评测 harness、轨迹日志与回放、以及 RL 训练/推理引擎的“同分布”对齐。<br><br> **可迁移结论** <br> 做可持续自改进，优先投“可执行任务集 + 验真 + rejection sampling + RL”的闭环；先从单一业务域做出确定性 verifier，再扩展到 rubric/judge。 |

### 2. Post-Training (The Chisel)

| 主题 | 整体趋势/演进 | 核心关注论文 (Key Papers) | 实践参考价值 |
| :--- | :--- | :--- | :--- |
| 训练范式 | **演进轴 1（监督信号形态）**：从「SFT 的离线 token-label 拟合」→「蒸馏式 dense token-logit 信号」→「把环境/反馈 token 化后的密集 credit assignment」，驱动来自“稀疏奖励与离线分布偏移”的长期瓶颈。 <br><br> **演进轴 2（on-policy 化）**：从「teacher 分布 / 数据分布上训练」→「student rollout 上训练（on-policy distill / self-distill）」以缓解暴露偏差与灾难性遗忘，约束来自稳定性（teacher 漂移）与算力（rollout 开销）。 <br><br> **演进轴 3（“更难”与“更像 RL”）**：从「追 offline 指标最优」→「让 offline 阶段对齐 online occupancy / 让训练集中覆盖更难样本」→「难度感知的优化与数据扩展形成闭环」，驱动来自“离线好 ≠ 上线/后 RL 好”和“中等难度样本被算法/数据机制天然偏置”。 <br><br> **本月关键拐点/信号**：OPD/自蒸馏正在被系统化地解释为“带 KL 约束的 dense RL”，并进一步出现“reward extrapolation（让学生超老师）”与“rich feedback（用错误日志当监督）”两条可落地的增益路径。 <br><br> **未解问题**：自蒸馏与隐式奖励在强推理/长输出上容易引入长度偏置、稳定性与“伪进步”（学到格式/话术而非能力），最佳的 teacher 正则、难度调度与评测口径仍缺统一答案。 | [Self-Distillation Enables Continual Learning](https://arxiv.org/abs/2601.19897)  <br><br> 场景/问题：顺序 SFT 学新技能/新知识时灾难性遗忘严重；纯 RL 虽更 on-policy 但常缺显式 reward。 <br> 方法思路：用“带示例上下文的同一个模型”当 teacher，在 student 自己 rollout 上做 on-policy distill（SDFT）。 <br> 关键机制：demonstration-conditioned self-teacher、on-policy distillation、reverse KL、EMA teacher、context distillation-as-IRL。 <br> 证据与边界：在 skill learning 与 knowledge acquisition 上相对 SFT 提升新任务并显著降低遗忘；依赖较强 ICL，较小模型可能不如 SFT。<br> 可复用组件：teacher prompt=“给定示例→按示例意图解题”的模板 <br><br> [Harder Is Better: Boosting Mathematical Reasoning via Difficulty-Aware GRPO and Multi-Aspect Question Reformulation](https://arxiv.org/abs/2601.20614)  <br><br> 场景/问题：GRPO 对“难题”更新幅度被隐式压低；数据增强多停留在“改写”而非“增难”。   <br> 方法思路：MathForge=DGPO（难度感知 GRPO）+ MQR（多维度增难且保持答案不变）。 <br> 证据与边界：在多模型多基准上 DGPO、MQR 及组合均优于 GRPO；MQR 生成失败通常变成全错样本，训练中被当作“无有效梯度”过滤，但前提是 verifier 足够可靠。 <br> 可复用组件：难度定义=当前策略下该题组平均正确率；question-level weight；三种“增难但保答案”的改写 rubric。 <br><br> [Reinforcement Learning via Self-Distillation](https://arxiv.org/abs/2601.20802)   <br><br> 场景/问题：RLVR 只拿到标量奖励，credit assignment 极稀疏；但很多环境能返回“错误原因”的文本反馈（runtime error/评测解释）。 <br> 方法思路：SDPO：把“反馈+原尝试”喂给同一模型得到 self-teacher 分布，再把 self-teacher 的 next-token 分布蒸馏回 student，形成 dense logit-level 信号。 <br> 证据与边界：在 LiveCodeBench v6 等上提升 sample efficiency 与最终精度；强依赖 ICL 能力，弱模型可能不如 GRPO；反馈质量差会误导；teacher 更新需正则否则不稳。 <br> 可复用组件：把 advantage 从标量换成“teacher vs student 分布差”的 dense 优势。 <br><br>[Good SFT Optimizes for SFT, Better SFT Prepares for Reinforcement Learning](https://arxiv.org/abs/2602.01058)   <br><br> 场景/问题：离线 SFT 指标更强的 checkpoint，进入同配方 RL 后反而更差（offline/online 排名反转）。 <br> 方法思路：PEAR：用 OPE/重要性采样视角对 SFT loss 做重加权，纠正“behavior policy（离线数据分布）≠ target policy（RL rollout 分布）”的 occupancy mismatch。 <br> 证据与边界：在可验证推理游戏与数学任务上提升 post-RL（如 AIME-2025 pass@8 提升可达 14.6%）；需要能估计/记录行为策略概率。<br><br> [Self-Distilled Reasoner: On-Policy Self-Distillation for Large Language Models](https://arxiv.org/abs/2601.18734)  <br><br> 场景/问题：on-policy distillation 通常要更强外部 teacher；RLVR（如 GRPO）采样昂贵且 reward 稀疏；同时很多推理数据集有 ground-truth 解。 <br> 方法思路：OPSD：同一模型分成 teacher（看题+特权信息：标准解/CoT）与 student（只看题），在 student rollout 上最小化 per-token 分布差，用“解读标准解”替代外部 teacher。 <br> 证据与边界：在多数学竞赛基准上优于 SFT，且相对 GRPO 提升 4–8× token efficiency；需要“rationalization”能力，模型过小增益有限；full-vocab 需要更高显存。 <br> 可复用组件：teacher prompt=“基于标准解先 rationalize 再重写解法”的模板；优先 full-vocab distill（可用 top-K 近似）。 <br><br> [Learning beyond Teacher: Generalized On-Policy Distillation with Reward Extrapolation](https://arxiv.org/abs/2602.12125) <br><br> 场景/问题：标准 OPD 往往“学生≈老师”；缺少对 OPD 作为 RL 目标的可控杠杆（reward vs KL 权重、reference 选择）。 <br> 方法思路：G-OPD：引入 reward scaling $\lambda$ 与可选 reference；$\lambda>1$ 的 ExOPD 做“reward extrapolation”，在同尺 teacher 或多 teacher 合并时可让学生超越老师边界。 <br> 证据与边界：在数学/代码上 ExOPD 相对 OPD 稳定提升；过大 $\lambda$ 可能不稳并引入长度偏置/隐式奖励 hacking；reward correction 需额外模型与算力。 <br> 可复用组件：把 OPD 训练暴露为两个旋钮：$\lambda$ 与 reference；对多 domain teacher 的能力合并用 ExOPD 做“超越式”蒸馏。 | **可复用的“方向性结论”**：<br><br> **把“可解释反馈”当作监督信号**：优先把环境反馈/错误日志 token 化，做 dense credit assignment，而不是只靠标量 reward 或离线答案拟合。 <br> **离线阶段要为 online rollout 做“分布对齐”而非只刷 offline**：用类似 PEAR 的 occupancy mismatch 纠偏思路，让离线学习更像未来 RL 会反复访问的轨迹。 <br> **训练与数据都要“向难而行”，但要可控**：用难度感知的优化（DGPO/DQW）与“增难但保答案”的数据扩展（MQR）组合，避免算法天然偏爱中等难度样本。 <br><br> **最小单元**：<br><br> **Self-teacher 双上下文模板**：同一模型构造 teacher（带示例/标准解/反馈上下文）与 student（不带），在 student rollout 上做 KL/JSD 蒸馏。 <br> **难度度量 + 问题级权重**：难度≈当前策略下该题组平均正确率的负值。 <br> **offline-to-online 纠偏权重**：对离线 SFT token loss 乘以“suffix likelihood ratio”（token/block/sequence 级），并做 log-space + clipping 稳定。 <br><br> **数据飞轮**：把现有的“失败尝试 + 反馈”（如标签）转成 tokenized feedback，作为 self-teacher 上下文并沉淀为训练时的 dense 蒸馏信号与难度估计信号。<br><br> **风险提示**：<br><br> **稳定性与偏置**：teacher 不正则会放大噪声/长度偏置，出现训练不稳或“隐式奖励 hacking”。 <br> **反馈质量与评测口径**：rich feedback 若不准/不一致，会把错误解释当真理；同时需防“话术伪影/格式学习”造成指标虚高。 |
| RL Reward | **演进轴 1（“reward 对不对”→“reward 为什么对”）**：从「只看 outcome accuracy / 二元偏好是否预测对」→「要求 judge / GenRM 的*推理过程*与人类理由一致（rationale consistency）」；驱动来自“deceptive alignment：判对但理由错，RL 时泛化与可控性差”。<br><br>**演进轴 2（稀疏/退化信号→可持续的有效梯度）**：从「RLVR 中大量提示在训练后期变成 solve-all（pass-rate=1）导致 advantage=0、有效 batch 变小」→「通过可验证的*组合式难题生成*把 easy prompts 变 hard prompts，并用课程式加深组合深度」<br><br>**演进轴 3（rubric 当说明书→rubric 当控制面/攻击面）**：从「rubric 只是评测/对齐的自然语言规范」→「rubric 是可编辑的偏好接口，会在*基准不降*的情况下对目标域产生系统性漂移 <br><br>**本月关键拐点/信号**：reward 讨论正在从“提升 judge 准确率”转向“过程一致性（可解释可校验）+ 数据难度可持续 + rubric 治理”的系统工程化组合。  <br><br>**未解问题**：如何在不显著增加人力成本的前提下规模化“高质量理由/核对清单”，并同时防住“rationale 话术化/模板化”与“rubric 漂移”在 RL 闭环中的放大效应。 | [Outcome Accuracy is Not Enough: Aligning the Reasoning Process of Reward Models](https://arxiv.org/abs/2602.04649v1)<br><br>场景/问题：GenRM/LLM-judge 可能“判对但理由错”，outcome accuracy 会掩盖这种 deceptive alignment，导致 RLHF 泛化差、可控性差。<br><br>方法思路：提出 **Rationale Consistency** 与 **METAJUDGE**，把人类理由拆成原子清单并做严格一对一语义匹配；训练时用“理由一致性 × outcome 正确”的混合奖励做门控式优化。<br>证据与边界：在 RM-Bench 达到 87.1%、JudgeBench 82%，较 outcome-only 平均提升约 5%；用于 RLHF 在 Arena Hard v2（创作写作）提升约 7%；但计算 RC 依赖高质量人类理由数据，且仅用 rationale reward 会出现 reward hacking（需要 outcome 门控）。<br>可复用组件：把人工评审意见结构化为“3–7 条原子 checklist”；用 LLM 做一对一匹配得到 $0\sim1$ 的过程分；训练/评测都同时跟踪 outcome 与 rationale consistency，防“只涨 outcome 的伪进步”。<br><br>[Composition-RL: Compose Your Verifiable Prompts for Reinforcement Learning of Large Language Models](https://arxiv.org/abs/2602.12036)<br>场景/问题：RLVR 训练中大量 prompt 在后期变为 solve-all（或 solve-none），GRPO/PG 的 advantage 变 0，动态采样后有效数据规模显著缩水，训练更易饱和。<br>方法思路：用 **Sequential Prompt Composition (SPC)** 自动把多个可验证题组合成更难的新题（仍可验证），以“把 easy prompts 重新变成有梯度的 prompts”，并用 curriculum 逐步提高组合深度 $K$。<br>证据与边界：在 4B–30B 多尺寸上，Composition-RL 相比原始 prompts RL 在多数学/多任务基准稳定提升；并展示跨域（物理+数学）组合比简单混训更有效；前提是 verifier 对组合题仍可靠，且组合生成需过滤错误样本。<br>可复用组件：可复用的“组合算子”与三段式校验（修改 $q_1$、修改 $q_2$、连接一致性）；训练中用 solve-all ratio 作为“数据变钝”的告警指标；课程式从 Depth 1→2→3 触发继续增难。<br><br>[Rubrics as an Attack Surface: Stealthy Preference Drift in LLM Judges](https://arxiv.org/abs/2602.13576)<br>场景/问题：LLM-judge 常靠自然语言 rubric 定义偏好；即使 rubric 改动在 benchmark 上验证通过，也可能在目标域产生系统性偏好漂移（RIPD），并在用作偏好标注/对齐时被下游模型“内化”。<br>方法思路：定义 **Rubric-Induced Preference Drift (RIPD)**；展示仅通过 rubric 编辑即可进行“rubric-based preference attacks”，在保持 benchmark 通过的同时定向劣化 target agreement。<br>证据与边界：可在 helpfulness/harmlessness 任务上把 target-domain 准确率最多降低约 9.5% / 27.9%，且可跨不同 judge 迁移；rubric 在盲测中并不一定“更差”，漂移可更隐蔽；说明静态 benchmark 验证不足以保证对齐安全。<br>可复用组件：把 rubric 当“reward policy 的代码”做版本管理与红队搜索；强制 benchmark/target/online 三套分布的漂移监控；对 rubric 变更做差分审计 + 多 judge 交叉一致性检查，减少单点评审失灵。 | **可复用的“方向性结论”**：<br><br> **Reward 不能只优化结果指标，要把“理由一致性”纳入训练与验收**：把 outcome accuracy 当必要不充分条件，用 rationale consistency 类指标约束 judge/GenRM 的判定逻辑，降低“判对但不可泛化”的风险。<br>  **把“有效梯度”当作数据资产来经营**：当 solve-all/solve-none 增多时，优先通过可验证的组合增难与课程深度来恢复信息量，而不是盲目加 rollout 或只追 hardest-only。<br>  **把 rubric 当高风险控制面治理**：任何 rubric 迭代都可能在目标域产生定向漂移，并在对齐训练中被放大；需要像治理“reward 代码”一样治理 rubric。<br><br>**最小单元**：<br><br>  **原子化理由 checklist + 一对一匹配评分**：将评审理由沉淀为 3–7 条可核对项，并用严格匹配产出过程分，再与 outcome 做门控混合奖励。<br> **SPC 组合算子 + 三段式自校验过滤**：用“修改 $q_1$ / 修改 $q_2$ / 连接约束”生成新题，并在每步做一致性校验，保证 verifier 仍可用。<br><br>**数据飞轮**：把现有的“偏好对比 + 评审理由/备注/反馈”结构化成原子 checklist，持续收集 judge 分歧与失败样本做补标；同时从既有可验证题库自动生成 compositional prompts，把“变强后变简单的数据”再变难，持续供给 RL 信号。<br><br>**风险提示**：<br><br> **rationale 话术化/对齐错位**：模型可能学会输出“看似专业的理由”但不做真正核查；需要严格一对一匹配与 evidence-grounded 约束，避免过程分被灌水。<br> **组合数据的“伪难/验错”问题**：若组合生成或 verifier 在新分布下不可靠，会引入系统性噪声；保留过滤与抽样人工 spot-check 的质量闸门。 |

### 3. Agent & Personalization (The Hands)

| 主题 | 整体趋势/演进 | 核心关注论文 (Key Papers) | 实践参考价值 |
| :--- | :--- | :--- | :--- |
| Agent 架构 | **演进轴 1（“能答对”→“能搜全且能停”）**：从「单点检索/单答案 QA 的 opportunistic browsing」→「面向 *set-based* 的系统性探索 + 去重/实体消歧 + 停止条件推理」<br><br>**演进轴 2（外置工作流→能力内化）**：从「靠工作流把模型串起来（ReAct/RAG/工具链堆叠）解决长链检索」→「用冷启动轨迹 + RL 把长时程、多轮工具交互内化进模型（尤其是多模态场景）」<br><br>**演进轴 3（单体脚本→资源约束下的模块化搜索）**：从「生成 monolithic 脚本、忽略评测成本与归因」→「把研究/工程当作 *repository-level* 搜索：预算感知规划（MCTS）+ 模块化实现（Design-Decompose-Implement）+ 反思记忆做 credit assignment」<br><br>**本月关键拐点/信号**：出现“可度量的深度研究基准（DeepSearchQA）+ 多模态深研训练范式（Vision-DeepResearch）+ 成本约束的模块化搜索框架（MARS）”三件套，架构讨论从“会不会用工具”转向“可持续长时程、可控成本、可验收 completeness”。<br><br>**未解问题**：如何在不爆炸式增加 tool-call 成本的前提下，可靠地学习/校准“停止条件”（避免 under-retrieval 与 over-retrieval/hedging 两头栽），并能在噪声搜索与跨域分布变化下稳定泛化。 | [DeepSearchQA: Bridging the Comprehensiveness Gap for Deep Research Agents](https://arxiv.org/abs/2601.20975)<br><br>场景/问题：现有 agent 基准多是单答案或偏 precision，难覆盖真实“深度研究”里对 *全面性*（exhaustive answer set）的需求，导致 *comprehensiveness gap* 难以被量化与诊断。<br>方法思路：提出 900 prompt 的 DeepSearchQA，用 set-based 的 Precision/Recall/F1（并有“Fully Correct/Correct with Extraneous”等严格类别）做 outcome-only 评分，把 agent 当黑箱但强约束“搜全 + 别瞎编 + 会停”。<br>证据与边界：实验显示 SOTA deep research agent 仍会在“最后一公里”出现 under-retrieval 或 hedging（召回高但夹杂多余项），且 outcome-only 虽鼓励架构多样性但缺少过程诊断；同时基准有静态网页/可复现性的设计取舍。<br>可复用组件：把业务任务改写为“答案集合”验收；引入 set-based 指标与“多余项惩罚”；把 failure mode 显式分桶（提前停/过度撒网/实体去重失败）。<br><br>[Vision-DeepResearch: Incentivizing DeepResearch Capability in Multimodal Large Language Models](https://arxiv.org/abs/2601.22060)<br><br>场景/问题：多模态深研在真实搜索里命中率低且噪声重，单次全图/少量查询的检索假设不成立；现有数据多是短轨迹，导致模型在复杂题上过早终止、搜索深度/广度不足。<br>方法思路：提出“多轮 × 多实体 × 多尺度”的视觉裁剪检索 + 文本深研桥接：先用多尺度 crop 反复打视觉搜索/网页访问/摘要与验证，再把轨迹桥接到强文本深研模型生成长链文本搜索轨迹；训练上用冷启动轨迹 SFT + 在线 RL（GRPO），并设计高吞吐异步 rollout 工程以支撑长时程交互。<br>证据与边界：在多项多模态事实类基准上显著提升，消融显示 multi-scale cropping 与 text search 组合更稳；但依赖外部搜索工具链与 judge/过滤策略的稳定性，长时程 RL 仍面临退化循环/格式与工具调用错误等工程风险。<br>可复用组件：multi-scale region proposal → 视觉检索 → 摘要+图文一致性验证；用“外部 judge”做视觉检索深度的终止门控；把长链 tool-use 训练拆成“可验证冷启动轨迹 + 在线 RL 微调”。<br><br>[MARS: Modular Agent with Reflective Search for Automated AI Research](https://arxiv.org/abs/2602.02660)<br><br>场景/问题：自动化 AI 研究不同于一般 coding：评测（训练/实验）昂贵、性能变化归因不透明；通用 LLM agent 易产出 monolithic 方案，既不控成本也难迭代归因。<br>方法思路：把研究过程建模为“在预算约束下搜索最优仓库”的问题：用 cost-constrained 的 Budget-Aware MCTS 做资源感知规划，用 Design-Decompose-Implement 做模块化仓库构建与验证，并用 Comparative Reflective Memory 通过“分支间解的差异”沉淀高信号 lessons，辅以 diff-based editing 做局部更新。<br>证据与边界：在 MLE-Bench 上开源框架中达到 SOTA 且与榜单强方法具竞争力，并报告大量“跨分支迁移”的 lessons 被复用（体现可泛化的反思记忆）；但前提是评测脚本可靠、预算/成本建模合理，且 MCTS/记忆上下文会带来额外系统复杂度。 | **可复用的“方向性结论”**：<br><br> **把“深研任务”验收从长文质量改成 set-based outcome 指标**：“列全/枚举/盘点/”类需求，优先用 Precision/Recall/F1 与“多余项”分桶来牵引架构迭代，而不是只看单点正确率或主观长文评分。<br><br> **长时程能力不要只靠编排，要通过“轨迹 + RL”内化关键 tool-use 行为**：尤其在噪声检索与多模态场景，需要把“多轮试错检索、证据过滤、终止门控”等变成可学习的策略，而不仅是工作流脚本规则。<br><br>**最小单元**：<br><br> **Set-answer 评测口径**：答案抽取→语义去重/实体消歧→P/R/F1 + “Correct with Extraneous/Partial”分桶，专门打击 hedging/过度撒网。<br><br> **多尺度检索 loop + 终止门控**：region proposal（多实体/多尺度）→ visual/text search → 网页访问/摘要 → 证据一致性验证 → judge-hit 决定继续/停止。<br><br>**风险提示**：<br><br> **过度追求 recall 导致 hedging/幻觉列表**：模型可能用“多给几个”刷召回，用“多余项”强惩罚与实体级去重/校验把 precision 拉住。<br><br> **长时程 tool-use 的成本与退化循环**：多轮检索/RL 容易出现重复、格式/工具调用错误与 wall-clock 长尾，需预算上限、重复检测、错误计数熔断与异步/并发工程化能力做兜底。 |
| Agent 自我演进与进化 | **演进轴 1（“记忆存档”→“技能抽象”→“递归进化”）**：从「把成功/失败轨迹原样塞进 memory（冗余+噪声+token 胀）」→「把经验蒸馏成可检索、可复用的 *SkillBank*（general vs task-specific）」→「在 RL 过程中用失败分布触发 skill 增量更新，让 *skill 库与 policy 共进化*」。<br><br>**演进轴 2（“会不会写 skill”→“skill 到底有没有用”→“什么时候会害你”）**：从「默认 Skills 一定提升」→「必须做 *paired evaluation*：no-Skills / curated-Skills / self-generated-Skills，把收益与失败模式量化」→「进一步收敛到 authoring 规律：2–3 个 focused skills 往往优于 comprehensive 文档；且存在显著负增益任务」。<br><br>**演进轴 3（单体 agent 的 skill 增强→多 agent 的 skill 迁移与编排）**：从「给单一 agent 喂技能」→「把 skill 当作路由/编排的中间表示：显式建模 *agent-在-技能维度* 的能力与成本，在多轮交互中按 performance–cost 权衡选择合适 agent，避免 RL-router 的高代价与 routing collapse」。<br><br>**本月关键拐点/信号**：Skill 开始成为“自我演进”的统一接口层——上游用 skill 抽象经验（更可控/更省 context），中游用 benchmark 定义有效性与负增益边界，下游用 skill 画像做编排与成本治理。 <br><br>**未解问题**：如何规模化“高质量、不过拟合、不泄漏”的技能生成与治理，并在真实线上分布下持续监控 *skill 冲突/过载/失效* 与路由误判带来的系统性退化。 | [SkillRL: Evolving Agents via Recursive Skill-Augmented Reinforcement Learning](https://arxiv.org/abs/2602.08234) <br><br>场景/问题：memory-based 方法存 raw trajectories，冗余与噪声大，难以抽出可迁移的高层规律，且 agent 往往“每次任务都从头来”。<br><br>方法思路：用 teacher 将成功/失败轨迹差异化蒸馏成结构化技能，构建分层 SkillBank（general + task-specific），推理时自适应检索；训练时先 cold-start SFT 教会“如何用 skill”，再用 GRPO 做 RL，并在每轮验证后对低表现类别触发 *recursive skill evolution*（新增/改写技能）。<br><br>证据与边界：在 ALFWorld、WebShop 与 7 个 search-augmented 任务上取得 SoTA，报告较强基线平均提升 15.3%，并宣称相对 raw trajectory 具 10–20× token 压缩；但依赖 teacher 蒸馏与 skill 质量、以及“失败分析→技能更新”的触发与过滤策略是否稳定。 <br><br>可复用组件：成功保留“关键决策点+理由”、失败改写成“failure lesson（错因+该怎么做+通用原则）”；skill schema（name / principle / when-to-apply）+ general/task-specific 分层；类别级阈值触发的技能迭代回路（用验证失败驱动 skill 增量）。<br><br>[SkillsBench: Benchmarking How Well Agent Skills Work Across Diverse Tasks](https://arxiv.org/abs/2602.12670) <br><br>场景/问题：Skills 生态快速增长，但缺少标准化方法回答“skills 是否真的提升、对谁提升、在哪些任务会变差”。<br><br>方法思路：构建 86 个任务（评测时 84 个）跨 11 域的容器化 benchmark，配套 curated skills 与 deterministic verifiers；每个任务在 no-Skills / curated-Skills / self-generated-Skills 三条件下评测，以 *paired evaluation* 直接量化技能增益，并加入 leakage prevention 与贡献规范（skills 必须是 procedural、面向任务类、不可含 task-specific 解）。<br><br>证据与边界：curated skills 平均 +16.2pp，但跨域差异巨大（如软件工程 +4.5pp、医疗 +51.9pp），且 16/84 任务出现负增益；self-generated skills 平均无收益（-1.3pp）；2–3 个 focused skills 通常优于 4+ 或 comprehensive 文档；skills 可部分替代模型规模。 <br><br>可复用组件： 三条件（no/curated/self-gen）作为“技能有效性验收”标准工位； deterministic verifier + container 让回归可重复； authoring 规则（少而精、可执行资源/示例、反泄漏审计）。<br><br>[SkillOrchestra: Learning to Route Agents via Skill Transfer](https://arxiv.org/abs/2602.19672) <br><br>场景/问题：复合系统的路由/编排中，input-level router 过粗，RL-orchestrator 代价高且易 routing collapse（反复调用最强最贵选项），难以适配不断变化的 agent/tool 池。 <br><br>方法思路：把编排改写为 skill-grounded 决策：从执行经验中学习细粒度技能表示，并显式建模“每个 agent 在各技能上的胜率与成本”，部署时推断当前交互所需技能，在明确的 performance–cost 目标下选择 agent；并提出可复用的 Skill Handbook/agent profiles 等中间资产以支持迁移与可解释路由。 <br><br>证据与边界：在 10 个 benchmark 上，相比 RL-based orchestrators 最高提升 22.5%，且相对 Router-R1 / ToolOrchestra 报告 700× / 300× 的学习成本降低；但效果依赖 skill 粒度选择、技能识别准确性与画像统计是否覆盖线上分布。 <br><br>可复用组件： “技能需求→agent 选择”的显式打分（能力×成本权衡）； agent profile（按技能聚合成功率/成本统计，如 Beta 后验）； skill 粒度的 split/merge 与验证集 Pareto 选择思路，用于避免过细/过粗导致的路由误判。 | **可复用的“方向性结论”**：<br><br> 把 Skill 当“中间表示/可治理资产”，不要把“经验”直接当上下文堆叠：先抽象为小而可复用的 procedural 单元，再进入训练/推理/编排闭环。 <br><br> Skills 必须做 *paired evaluation* 才能上线：同一任务分布下长期跑 no-Skills vs curated-Skills（必要时加 self-gen 作为下限对照），把“负增益任务”标为需治理，而不是平均值掩盖。 <br><br> 多 agent/多模型体系里，优先用“显式技能画像 + 成本约束”的路由替代端到端 RL 路由，并监控 routing collapse。 <br><br>**最小单元**：<br><br> Skill 文档与结构：`name + principle + when_to_apply`（配 1 个可执行模板/脚本/示例），并区分 general skills 与 task-specific skills。 <br><br> 评测口径：`no-Skills / curated-Skills / self-generated-Skills` + deterministic verifier（容器化/可回归），并记录 task-level Δ（含负增益清单）。 <br><br> 编排画像表：按 skill 维度沉淀 `agent -> {success_prob, cost}` 的 profile，并把路由目标写成显式的 performance–cost trade-off（可做 Pareto 选择）。 <br><br>**风险提示**：<br><br> Skill 过载/冲突导致负增益：skills 数量过多或文档过“大全”会引入认知与上下文成本，甚至让部分任务变差；需要“少而精 + task-level 回归 + 负增益红线”。 <br><br> 质量与分布漂移：self-generated skills 往往无效，自动生成/更新若缺少过滤与审计，可能把错误流程固化；编排侧也可能因技能识别误差或画像覆盖不足而路由退化。 |
| 个性化建模 | **演进轴 1（离线画像 → 在线“可纠错”的个性化）**：从「用静态历史日志/一次性 profile 做个性化」→「把交互本身当学习信号：先问清楚（pre-action）+ 做错就纠正（post-action）+ 显式、可更新的 per-user memory」。<br><br> **演进轴 2（只管输出像不像 → 连“为什么这样演”也个性化）**：从「角色口吻/知识对齐（surface persona）」→「把角色的内心动机/计划作为可监督与可优化对象（dual-layer thinking），并用偏好对齐的 reward 约束“角色思考方式”而不仅是台词」。<br><br> **演进轴 3（单一打分器 → 可解释、可组合的“原则+对比”评判）**：从「一个黑箱分数/固定 rubric」→「生成式 reward：按 case 生成原则（by-case principles）+ 维度化对比分析 + 最终偏好判决，并用数据配比治理 reward hacking/模式塌缩」。<br><br> **本月关键拐点/信号**：个性化不再被当作“加点记忆/RAG”或“persona prompt 工程”，而是被系统化为“可持续闭环”（澄清—执行—纠错—写入/改写记忆）与“可评判的内在过程”。<br><br> **未解问题**：如何在**低打扰**与**低隐私风险**前提下，让记忆更新既能覆盖偏好漂移、又不被噪声/话术/reward hacking放大。 | [Learning Personalized Agents from Human Feedback](https://arxiv.org/abs/2602.16173) <br><br> 场景/问题：面向真实用户的 agent 往往遇到三类硬问题：新用户冷启动、做错后学不会（不能实时纠错）、偏好随时间漂移导致“自信地错”。<br> 方法思路：提出 **PAHF**：三步闭环—— 行动前检索记忆，必要时主动澄清（pre-action）；基于检索到的偏好执行动作；行动后把纠错反馈写回/改写显式 per-user 记忆（post-action）。<br> 证据与边界：构建四阶段协议（初始学习/初始测试/漂移后再学习/漂移后测试）与两个域（具身操作、在线购物），显示“只 pre-action”在偏好漂移下会因为不再感知歧义而卡死，“只 post-action”虽能追上但用户成本更高；两者结合在 SR/ACPE 等指标上最好。<br> 可复用组件：**用户隔离**的“短自然语言 note + embedding”记忆条目；写入Pipeline **detect（是否有用）→ summarize（抽取偏好）→ integrate（相似则 update，否则 add）**；漂移压力测试：Phase-1/3 输入相同但标签翻转，用来检验“能否改写旧认知”。<br><br> [HER](https://arxiv.org/abs/2601.21459) [Source](https://arxiv.org/pdf/2601.21459v3) <br><br> 场景/问题：LLM 角色扮演常能模仿语气，但难以稳定地“像人一样在角色里推理”，且开放式输出缺少可靠 reward，易被表层特征（长度/位置/固定模板）劫持。<br> 方法思路：提出 **Dual-layer Thinking**（隐藏的第三人称 system thinking + 可见的第一人称 role thinking），并用“逆向合成”生成带推理轨迹的数据；再训练**生成式** Role-play GRM（按 case 生成原则+对比分析+偏好判决）做 RL 的偏好信号。<br> 证据与边界：基于 Qwen3-32B 训练，报告在 CoSER 与 Minimax Role-Play Bench 上显著提升；同时给出 reward 可靠性与防hacking细节（by-case principles、pairwise、CoT、balanced construction、pattern collapse 监控）。<br> 可复用组件：“system vs role”双层思考的**输出协议/标签体系**；从对话反推推理的**三阶段合成**（role thinking augmentation → system thinking construction → integration/context augmentation）；GRM 训练的**原则蒸馏+平衡数据配比**（用于降低 position/length/pattern bias）。 | **可复用的“方向性结论”**：<br><br> 把个性化当成“闭环控制”而不是“画像特征”——**先澄清再行动**降低冷启动误差，**做错必更新**解决漂移下的“自信地错”。<br><br> 把“个性化的内在过程”纳入对齐目标——对陪伴/剧情/多轮交互类业务，仅优化台词风格不够，需要把角色动机/计划（role thinking）作为稳定性与可信度的抓手，并让 reward 能解释“为何更像这个人/这个角色”。<br><br> 评判体系要可治理——偏好判断建议走“按 case 生成原则 + 维度化对比 + 最终判决”的可审计形态，并用数据配比与模式塌缩指标去防 reward hacking。<br><br> **最小单元**：<br><br> **Per-user preference memory schema**：`note(text) + embedding + timestamp + scope(场景/条件) + confidence + source(pre/post)`，并强制用户隔离与可回滚版本化（便于漂移与误写纠正）。<br><br> **写入闸门（salience detector）+ 近重复合并（update vs add）**：先判“是否包含可行动的偏好信息”，再抽取成短 note，最后按相似度阈值合并或新增，避免记忆膨胀与冲突扩散。[Source](https://arxiv.org/html/2602.16173v1) <br><br> **双层思考输出协议**：system thinking（隐藏、不进入对话历史/不参与评测）+ role thinking/action/speech（可见、可评测），用于把“角色内在一致性”变成可训练对象。<br><br> **风险提示**：<br><br> **记忆“写错/写过期”会系统性放大**：只靠 pre-action 容易在写入后不再提问、漂移时卡死；需要把 post-action 纠错作为硬机制，并为冲突 note 设计覆盖/作废策略与审计能力。<br><br> **reward/judge 被话术与模式劫持**：GRM/偏好模型会产生位置偏置、长度偏置、维度比较“全 A/全 B”的 pattern collapse；需要平衡数据、混合格式、以及对 collapse 的在线监控与回归测试。 |

---

## 🎨 Fresh Sketches (Monthly Summary)

* **Crucial Insight:** [本月最大的认知变化是什么？例如：意识到 RLHF 中数据质量远比算法本身重要。]
* **Best Implementation:** [本月发现的最佳开源实现，例如 vLLM 的某个新 Kernel。]

---
